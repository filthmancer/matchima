using UnityEngine;
//using UnityEditor;
using System.Collections;

public class ConvertToQuoteGroup : MonoBehaviour {

	//[MenuItem ("Utility/Convert Hero Quotes To Quote Groups")]
	//static void ConvertQuoteGroups()
	//{
	//}
	//[MenuItem ("Utility/Convert Hero Quotes To Quote Groups", true)]
	//static bool ConvertQuoteGroupsValid()
	//{
	//	return Selection.activeTransform.GetComponent<Class>() != null;
	//}

	/*[MenuItem ("CONTEXT/Class/Convert Hero Quotes To Quote Groups")]
	static void ConvertQuoteGroupsContext(MenuCommand command)
	{
		/*Class c = (Class) command.context;
		int i = 0;
		foreach(Quote child in c.Quotes._Start)
		{
			QuoteGroup group = new QuoteGroup("Start " + i);
			group.AddQuote(child.Text, c, child.OverrideTouch, child.WaitTime);
			c.Quotes.StartQuotes.Add(group);
			i++;
		}

		i = 0;
		foreach(Quote child in c.Quotes._GoodBonus)
		{
			QuoteGroup group = new QuoteGroup("GoodBonus " + i);
			group.AddQuote(child.Text, c, child.OverrideTouch, child.WaitTime);
			c.Quotes.GoodBonusQuotes.Add(group);
			i++;
		}

		i = 0;
		foreach(Quote child in c.Quotes._Danger)
		{
			QuoteGroup group = new QuoteGroup("Danger " + i);
			group.AddQuote(child.Text, c, child.OverrideTouch, child.WaitTime);
			c.Quotes.DangerQuotes.Add(group);
			i++;
		}

		i = 0;
		foreach(Quote child in c.Quotes._Death)
		{
			QuoteGroup group = new QuoteGroup("Death " + i);
			group.AddQuote(child.Text, c, child.OverrideTouch, child.WaitTime);
			c.Quotes.DeathQuotes.Add(group);
			i++;
		}

		i = 0;
		foreach(Quote child in c.Quotes._Idle)
		{
			QuoteGroup group = new QuoteGroup("Idle " + i);
			group.AddQuote(child.Text, c, child.OverrideTouch, child.WaitTime);
			c.Quotes.IdleQuotes.Add(group);
			i++;
		}

		i = 0;
		foreach(Quote child in c.Quotes._Info)
		{
			QuoteGroup group = new QuoteGroup("Info " + i);
			group.AddQuote(child.Text, c, child.OverrideTouch, child.WaitTime);
			c.Quotes.InfoQuotes.Add(group);
			i++;
		}
	}*/
}
